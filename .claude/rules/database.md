---
globs:
  - "**/db/migration/**"
  - "**/entity/**"
  - "**/model/**"
---
# 数据库与实体类红线

## 🚫 绝对禁止事项
- **禁止修改历史**：绝对禁止修改、删除或重命名已提交/已执行的数据库迁移脚本（如 Flyway/Liquibase 的 `V1__xxx.sql`）。只能新增脚本。
- **禁止危险命令**：禁止在代码或脚本中生成 `DROP TABLE`、`TRUNCATE` 等毁灭性命令，除非我明确要求。
- **敏感信息**：禁止在实体类或配置中硬编码密码、密钥，必须使用环境变量。